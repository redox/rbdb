<% @title = @table.name %>

<%= render :partial => '/tables/submenu' %>

<object type="image/svg+xml" data="<%= datab_relations_graph_path(@datab, @table, :format => 'svg') %>"></object>

<%= link_to 'Back', datab_relations_path(@datab) %>
